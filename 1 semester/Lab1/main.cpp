#include <iostream>

#include "src/Data structures/Matrix_graph.h"

int main() {
    Matrix_graph<int, int> graph(false, false);

    graph.add_vertex(1);
    graph.add_vertex(2);
    graph.add_vertex(3);
    graph.add_vertex(4);
    graph.add_vertex(5);



}